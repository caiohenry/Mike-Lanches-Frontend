const api_url = 'localhost';

// ! Please don't change the code below, use the environment.ts file to change the environment variables.
export const environment = {

  production: false,
  apiWebSocket: 'ws://' + api_url + ':8080',
  api: 'http://' + api_url + ':8080'
};